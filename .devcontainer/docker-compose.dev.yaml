services:
  app:
    image: "mcr.microsoft.com/devcontainers/base:noble"
    volumes:
      # Mounts your local project folder to /workspace in the container
      - ../..:/workspaces:cached
    command: sleep infinity
